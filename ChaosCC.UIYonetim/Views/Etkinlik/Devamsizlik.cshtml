@model ChaosCC.Dto.DevamsizlikGridDto
@{
    ViewBag.Title = "Devamsizlik";
}

<h2>Devamsizlik</h2>
@using (Html.BeginForm("Devamsizlik", "Etkinlik", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Kaydet" class="btn btn-default" />
        </div>
    </div>
    @Html.HiddenFor(model => model.Id)
    <h2>@Html.DisplayFor(model => model.Etkinlik.EtlinlikTuru)</h2>
    <h2>@Html.DisplayFor(model => model.Etkinlik.Yer)</h2>
    @Html.DisplayFor(model => model.Etkinlik.Tarih)

    <table class="table">
        <tr>
            <th>
                Geldi
            </th>
            <th>
                Kullanıcı Adı
            </th>
            <th>
                Açıklama
            </th>
        </tr>

        @for (int i = 0; i < Model.Grid.Count; i++)
        {
            <tr>
                <td>

                    @Html.HiddenFor(m => Model.Grid[i].Id)
                    @Html.HiddenFor(m => Model.Grid[i].KullaniciId)
                    @Html.CheckBoxFor(m => Model.Grid[i].Geldi)

                </td>
                <td>
                    @Html.DisplayFor(m => Model.Grid[i].KullaniciAdi)
                </td>
                <td>
                    @Html.TextBoxFor(m => Model.Grid[i].Aciklama)
                </td>
            </tr>
        }

        @*@foreach (var item in Model.Grid)
                {
                    <tr>
                        <td>

                            @Html.HiddenFor(m => item.Id)
                            @Html.HiddenFor(m => item.KullaniciId)
                            @Html.DisplayFor(m => item.KullaniciAdi)
                        </td>
                        <td>
                            @Html.CheckBoxFor(m => item.Geldi)
                        </td>
                        <td>
                            @Html.TextBoxFor(m => item.Aciklama)
                        </td>
                    </tr>
            }*@

    </table>
}